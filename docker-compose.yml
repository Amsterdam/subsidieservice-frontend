version: "3.7"
services:
  subsidy_frontend:
    build:
      dockerfile: ./Dockerfile
      context: .
      args:
        VUE_APP_API_URL: "http://localhost:8080/api/v1"
    ports:
      - "8081:80"

