version: '3'
services:
  subsidy_frontend:
    image: 'subsidy/frontend'
    container_name: subsidy_frontend_dev
    ports:
      - '8081:80'
    build:
      dockerfile: ./Dockerfile
      context: .
    environment:
      ENV:            "develop"
    env_file:
      ".env"
      